import { UserListState } from './states/user-list.states';

// import {RouterState} from '@ngrx/router-store';

export interface State {
  userList: UserListState;
  
//   router: fromRouter.RouterState;
}